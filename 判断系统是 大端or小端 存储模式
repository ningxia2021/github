#include<stdio.h>
void main() {

	//判断系统 大/小 端存储模式
	//判断的关键,第一个数据放在内存地址高位,还是低位
	//为了简单一点,我们就放1,然后取内存的低地址,是0则是大端存储,是1 则是小端存储

	int i = 1;
	char* p = &i;
	if (*p == 1)
	{
		printf("小端存储");
	}else{
  		printf("大端存储");
  }
}
